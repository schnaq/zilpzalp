The wordmark, set in type because no logo artwork exists yet — "Zilp" leaf green, "Zalp" hoopoe orange.

```jsx
<Wordmark size={88} />
<Wordmark size={40} tone="mono-light" />
```

Never restyle, outline, rotate, or add a bird glyph to it. If real logo art arrives, swap this component's internals and keep the API.
