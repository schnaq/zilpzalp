Soft pill label for counts, habitats and rarity — decorative only, never tappable.

```jsx
<Badge tone="rare" icon="sparkles">Selten</Badge>
```

Use `rare` (berry) only for genuinely rare finds, so it keeps its meaning.
