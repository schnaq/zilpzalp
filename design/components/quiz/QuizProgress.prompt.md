Wordless round progress — one leaf per question, filled green when answered.

```jsx
<QuizProgress total={5} done={2} current={2} />
```

Never show numbers or percentages to kids; the leaves are the whole story. Sits in the TopBar centre.
