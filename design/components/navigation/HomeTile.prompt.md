Square activity tile ("nest") on the home tree, with 0–3 earned stars.

```jsx
<HomeTile icon="volume-2" label="Wer singt da?" tone="leaf" stars={2} onOpen={start} />
<HomeTile label="Bald!" locked />
```

Locked tiles show an egg — waiting to hatch, not forbidden.
