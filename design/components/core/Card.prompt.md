Rounded, outlined panel that groups content on a cream page.

```jsx
<Card tone="leaf" pad="var(--space-6)">Heute gelernt: 3 Vögel</Card>
```

Always keeps its 3px outline — the outline, not the shadow, is what makes it read as illustration. Use tints sparingly: one tone per group.
