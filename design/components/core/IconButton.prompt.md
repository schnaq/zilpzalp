Round, wordless control for navigation and utilities (back, home, replay, settings) — the only icon-only control kids ever touch.

```jsx
<IconButton icon="chevron-left" label="Zurück" tone="quiet" size={96} />
```

Always pass `label`. Minimum `size` 64; 96 for anything a child taps mid-game.
