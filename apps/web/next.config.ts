import type { NextConfig } from 'next'

// Deliberately empty. The site is static German copy: no image optimisation, no
// rewrites, no external origins. Anything added here would be another thing to
// keep in step with the Kids Category rules the app is built to.
const nextConfig: NextConfig = {}

export default nextConfig
